/**
 * Canonical hex samples mirrored by `tokens.css` — keep in sync for contrast tests.
 * Soft portal-green lab (not neon garish); triad adapted from gold/cyan/magenta.
 */
export const V25_TOKEN_HEX = Object.freeze({
  bg: '#0a1210',
  panel: '#121a16',
  text: '#e8ffe8',
  textMuted: '#8aab95',
  accent: '#3ecf8e',
  onAccent: '#07110c',
});
