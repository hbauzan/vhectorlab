/**
 * Startup chrome defaults — single source for MODE | VIEW | RENDER.
 * Same triad on desktop and mobile (viewport only changes dock collapse).
 */
export const DEFAULT_WORKSPACE_MODE = 'ARITHMETIC';
export const DEFAULT_VIEW_MODE = 'ANALYSIS';
export const DEFAULT_RENDER_MODE = 'POINTS';
