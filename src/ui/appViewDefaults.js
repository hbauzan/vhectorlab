/**
 * Startup chrome defaults — single source for MODE | VIEW | RENDER.
 */
export const DEFAULT_WORKSPACE_MODE = 'COMPARE';
export const DEFAULT_VIEW_MODE = 'GALAXY';
export const DEFAULT_RENDER_MODE = 'POINTS';
